name = "divvy-realtime"
main = "src/index.ts"
compatibility_date = "2026-07-01"

# The presence room. SQLite-backed DO class (required for new namespaces / free plan).
[[durable_objects.bindings]]
name = "ROOM"
class_name = "PresenceRoom"

[[migrations]]
tag = "v1"
new_sqlite_classes = ["PresenceRoom"]
