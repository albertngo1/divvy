name = "divvy"
compatibility_date = "2026-07-01"
# Cloudflare Pages project: build command "npm run build", output dir "dist".
# Pages serves the output directory automatically — NO deploy command needed.
pages_build_output_dir = "dist"
